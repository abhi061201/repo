import 'package:flutter/material.dart';
import 'package:flutter/src/widgets/container.dart';
import 'package:flutter/src/widgets/framework.dart';

class JhoiceUpdatePostScreen extends StatelessWidget {
  
  const JhoiceUpdatePostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text('Jhoice Update Post Screen'), centerTitle: true,automaticallyImplyLeading: false,),
      body: Column(),
    );
  }
}